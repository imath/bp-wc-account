<?php
/**
 * wc_get_template() requires a template, else it throws a notice ??
 *
 * So let's load nothing to get rid of the navigation!
 */

// Exit if accessed directly
defined( 'ABSPATH' ) || exit;
